import 'package:flutter/material.dart';

class InnerContainer extends StatelessWidget {
  const InnerContainer({super.key, required this.child});
  final Widget child;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
